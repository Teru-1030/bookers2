<div class="containar">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <%= image_tag current_user.get_profile_image(100,100) %>
      <table>
        <tbody>
          <tr>
            <th>name</th>
            <th><%= current_user.name %></th>
          </tr>
          <tr>
            <th>introduction</th>
            <th><%= current_user.introduction %></th>
          </tr>
        </tbody>
      </table>
        <%= link_to(edit_user_path(current_user.id)) do %>

        <i class="fa-solid fa-user-gear"></i>

        <% end %>


         <h2>New Book</h2>

         <%= form_with model: Book.new do |f| %>
           <h4>Title</h4>
           <%= f.text_field :title %>

           <h4>Opinion</h4>
           <%= f.text_area :body %>

           <%= f.submit %>

         <% end %>
　  </div>
    <div class="col-md-8 offset-md-1">

      <h2>Book detail</h2>
       <table>
          <tbody>

           <tr>
             <td><%= image_tag @book_params.user.get_profile_image(100,100) %></td>
             <td><%= @book_params.title %></td>
             <td><%= @book_params.body %></td>
             <td><%=link_to "Edit", edit_book_path(@book_params) %></td>
             <td><%= link_to "Destroy", book_path(@book_params), method: :delete %></td>
           </tr>
         </tbody>
       </table>
     </div>

  </div>
</div>